spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: ctFire-service-redis,ctFire-client-redis,ctFire-client-admin,ctFire-client-eureka,ctFire-client-zipkin
      label: master
      profile: dev